# Supabase Configuration (OPTIONAL - app will use local JSON storage if not configured)
# Get these from: https://supabase.com/dashboard/project/_/settings/api
# Leave blank or use placeholder values to use local JSON storage for development
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Resend Configuration (OPTIONAL - for email notifications)
# Get from: https://resend.com/api-keys
RESEND_API_KEY=
# Email address to send notifications to (comma-separated for multiple)
RSVP_NOTIFY_TO=your-email@example.com
# Email address to send FROM (must be verified domain with Resend)
RSVP_NOTIFY_FROM=Our Wedding <noreply@yourdomain.com>

# Legacy support (will use RSVP_NOTIFY_TO if set)
NOTIFICATION_EMAIL=your-email@example.com

# Session ID for documentation
PAWGRAMMER_SESSION_ID=default_session
